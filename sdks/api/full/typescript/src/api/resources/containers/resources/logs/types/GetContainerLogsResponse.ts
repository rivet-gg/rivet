/**
 * This file was auto-generated by Fern from our API Definition.
 */

import * as Rivet from "../../../../../index";

export interface GetContainerLogsResponse {
    /** List of container IDs in these logs. The order of these correspond to the index in the log entry. */
    containerIds: Rivet.Id[];
    /** Sorted old to new. */
    lines: string[];
    /** Sorted old to new. */
    timestamps: Rivet.Timestamp[];
    /**
     * Streams the logs came from.
     *
     * 0 = stdout
     * 1 = stderr
     */
    streams: number[];
    /** List of flags denoting if this log is not directly from the container. */
    foreigns: boolean[];
    /** Index of the container that this log was for. Use this index to look the full ID in `container_ids`. */
    containerIndices: number[];
    watch: Rivet.WatchResponse;
}
