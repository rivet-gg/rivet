/**
 * This file was auto-generated by Fern from our API Definition.
 */

import * as serializers from "../../../../../index";
import * as Rivet from "../../../../../../api/index";
import * as core from "../../../../../../core";
import { NetworkMode } from "./NetworkMode";
import { Port } from "./Port";

export const Network: core.serialization.ObjectSchema<serializers.servers.Network.Raw, Rivet.servers.Network> =
    core.serialization.object({
        mode: NetworkMode.optional(),
        ports: core.serialization.record(core.serialization.string(), Port),
    });

export declare namespace Network {
    export interface Raw {
        mode?: NetworkMode.Raw | null;
        ports: Record<string, Port.Raw>;
    }
}
