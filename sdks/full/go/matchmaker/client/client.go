// This file was auto-generated by Fern from our API Definition.

package client

import (
	http "net/http"
	core "sdk/core"
	lobbies "sdk/matchmaker/lobbies"
	players "sdk/matchmaker/players"
	regions "sdk/matchmaker/regions"
)

type Client struct {
	baseURL string
	caller  *core.Caller
	header  http.Header

	Lobbies *lobbies.Client
	Players *players.Client
	Regions *regions.Client
}

func NewClient(opts ...core.ClientOption) *Client {
	options := core.NewClientOptions()
	for _, opt := range opts {
		opt(options)
	}
	return &Client{
		baseURL: options.BaseURL,
		caller:  core.NewCaller(options.HTTPClient),
		header:  options.ToHeader(),
		Lobbies: lobbies.NewClient(opts...),
		Players: players.NewClient(opts...),
		Regions: regions.NewClient(opts...),
	}
}
