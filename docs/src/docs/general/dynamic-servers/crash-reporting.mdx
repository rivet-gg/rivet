import imgLobbyCrash from './images/lobby-crash.png';

# Crash Reporting

If your server crashes, the status will show a red dot in the logs. Check both _stdout_ and _stderr_ for any error messages.

<Image src={imgLobbyCrash} alt='Dashboard for crashed lobby' />
