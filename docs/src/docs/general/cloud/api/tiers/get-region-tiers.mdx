
# tiers.getRegionTiers



## Description

Returns all available region tiers.


## Code Examples

<CodeGroup title='Request' tag='GET' label='https://api.rivet.gg/cloud/region-tiers'>

```bash {{ "title": "cURL" }}
curl -X GET 'https://api.rivet.gg/cloud/region-tiers'
```

```ts
// Create Rivet client
import { RivetClient } from '@rivet-gg/api';
const RIVET = new RivetClient({ token: '[YOUR TOKEN HERE]' });

// Make request
await RIVET.cloud.tiers.getRegionTiers({
  // Add your request body here
});
```

</CodeGroup>

## Request Parameters

## Response Body
<SchemaPreview schema={{"type":"object","properties":{"tiers":{"type":"array","description":"A list of region server tiers.","item":{"type":"object","description":"A region server tier.","properties":{"tier_name_id":{"type":"string","description":"A human readable short identifier used to references resources. Different than a `rivet.common#Uuid` because this is intended to be human readable. Different than `rivet.common#DisplayName` because this should not include special characters and be short."},"rivet_cores_numerator":{"type":"number","description":"Together with the denominator, denotes the portion of the CPU a given server uses."},"rivet_cores_denominator":{"type":"number","description":"Together with the numerator, denotes the portion of the CPU a given server uses."},"cpu":{"type":"number","description":"CPU frequency (MHz)."},"memory":{"type":"number","description":"Allocated memory (MB)."},"disk":{"type":"number","description":"Allocated disk space (MB)."},"bandwidth":{"type":"number","description":"Internet bandwidth (MB)."},"price_per_second":{"type":"any"}}}}}}}/>
