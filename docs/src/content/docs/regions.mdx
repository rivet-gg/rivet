# Available regions

<Info>
  This list can be fetched with `GET /regions`.
</Info>

- `atl` Atlanta, Georgia, USA
- `bom` Mumbai, India
- `fra` Frankfurt, Germany
- `gru` São Paulo, Brazil
- `lax` Los Angeles, California, USA
- `osa` Osaka, Japan
- `sin` Singapore
- `syd` Sydney, Australia

## Automatic region selection

Actors automatically choose the nearest region to the user if not provided.
