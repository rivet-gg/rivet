import { JsonSchemaPreview } from '@/components/JsonSchemaPreview';
import TOOLCHAIN_SPEC from './toolchain-spec.json';

# Config

The Rivet configuration file can use any fo the follow names:

| Name         | Format                                                                                              |
| ------------ | --------------------------------------------------------------------------------------------------- |
| `rivet.json` | JSON                                                                                                |
| `rivet.json` | JSON with comments ([JSONC](https://code.visualstudio.com/docs/languages/json#_json-with-comments)) |

## Schema

<JsonSchemaPreview className='not-prose' title='Config' schema={TOOLCHAIN_SPEC} />
