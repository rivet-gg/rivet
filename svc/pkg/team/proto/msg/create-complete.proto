syntax = "proto3";

package rivet.backend.pkg.team.msg.create_complete;

import "proto/common.proto";

/// name = "msg-team-create-complete"
/// parameters = [
/// 	{ name = "team_id" },
/// ]
message Message {
	rivet.common.Uuid team_id = 1;
}

