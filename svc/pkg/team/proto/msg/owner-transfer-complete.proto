syntax = "proto3";

package rivet.backend.pkg.team.msg.owner_transfer_complete;

import "proto/common.proto";

/// name = "msg-team-owner-transfer-complete"
/// parameters = [
/// 	{ name = "team_id" },
/// ]
message Message {
	rivet.common.Uuid team_id = 1;
}
