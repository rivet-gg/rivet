syntax = "proto3";

package rivet.backend.pkg.team.msg.update;

import "proto/common.proto";

/// name = "msg-team-update"
/// tail-ttl = 300
/// parameters = [
/// 	{ name = "team_id" },
/// ]
message Message {
	rivet.common.Uuid team_id = 1;
}

