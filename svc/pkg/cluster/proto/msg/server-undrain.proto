syntax = "proto3";

package rivet.backend.pkg.cluster.msg.server_undrain;

import "proto/common.proto";
import "proto/backend/cluster.proto";

/// name = "msg-cluster-server-undrain"
/// parameters = [
/// 	{ name = "server_id" },
/// ]
message Message {
    rivet.common.Uuid server_id = 1;
}
