syntax = "proto3";

package rivet.backend.pkg.cluster.msg.server_dns_create;

import "proto/common.proto";

/// name = "msg-cluster-server-dns-create"
/// parameters = [
/// 	{ name = "server_id" },
/// ]
message Message {
    rivet.common.Uuid server_id = 1;
}
