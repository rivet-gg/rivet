syntax = "proto3";

package rivet.backend.pkg.cluster.msg.create_complete;

import "proto/common.proto";

/// name = "msg-cluster-create-complete"
/// parameters = [
/// 	{ name = "cluster_id" },
/// ]
message Message {
    rivet.common.Uuid cluster_id = 1;
}
