syntax = "proto3";

package rivet.backend.pkg.cluster.msg.datacenter_tls_issue;

import "proto/common.proto";

/// name = "msg-cluster-datacenter-tls-issue"
/// parameters = [
/// 	{ name = "datacenter_id" },
/// ]
message Message {
    rivet.common.Uuid datacenter_id = 1;
    bool renew = 2;
}
