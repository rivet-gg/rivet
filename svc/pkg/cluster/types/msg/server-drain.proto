syntax = "proto3";

package rivet.backend.pkg.cluster.msg.server_drain;

import "proto/common.proto";

/// name = "msg-cluster-server-drain"
/// parameters = [
/// 	{ name = "server_id" },
/// ]
message Message {
    rivet.common.Uuid server_id = 1;
}
