syntax = "proto3";

package rivet.backend.pkg.cluster.msg.update;

import "proto/common.proto";
import "proto/backend/cluster.proto";

/// name = "msg-cluster-update"
/// parameters = [
/// 	{ name = "cluster_id" },
/// ]
message Message {
    rivet.backend.cluster.Cluster cluster = 1;
}
