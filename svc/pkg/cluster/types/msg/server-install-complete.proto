syntax = "proto3";

package rivet.backend.pkg.cluster.msg.server_install_complete;

import "proto/common.proto";

/// name = "msg-cluster-server-install-complete"
/// parameters = [
/// 	{ name = "public_ip" },
/// ]
message Message {
    // If set in request
    optional rivet.common.Uuid server_id = 1;
}
