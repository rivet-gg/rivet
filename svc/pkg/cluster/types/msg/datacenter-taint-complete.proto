syntax = "proto3";

package rivet.backend.pkg.cluster.msg.datacenter_taint_complete;

import "proto/common.proto";

/// name = "msg-cluster-datacenter-taint-complete"
/// parameters = [
/// 	{ name = "datacenter_id" },
/// ]
message Message {
    rivet.common.Uuid datacenter_id = 1;
}
