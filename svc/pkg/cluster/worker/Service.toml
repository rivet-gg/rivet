[service]
name = "cluster-worker"

[runtime]
kind = "rust"

[consumer]

[secrets]
"rivet/api_route/token" = {}
"cloudflare/terraform/auth_token" = {}
"ssh/server/private_key_openssh" = {}

[databases]
bucket-build = {}
