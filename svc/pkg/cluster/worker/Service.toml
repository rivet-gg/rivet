[service]
name = "cluster-worker"

[runtime]
kind = "rust"

[consumer]

[secrets]
"rivet/api_route/token" = {}
"cloudflare/terraform/auth_token" = { optional = true }
"ssh/server/private_key_openssh" = {}

[databases]
bucket-build = {}
