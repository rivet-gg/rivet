use chirp_worker::prelude::*;
use proto::backend::pkg::*;

#[worker_test]
async fn server_install(ctx: TestCtx) {
	// msg!([ctx] cluster::msg::server_install() {

	// })
	// .await
	// .unwrap();

	todo!();
}
