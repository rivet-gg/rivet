use chirp_worker::prelude::*;
use proto::backend::pkg::*;

#[worker_test]
async fn nomad_node_drain_complete(ctx: TestCtx) {
	// msg!([ctx] cluster::msg::nomad_node_drain_complete() {

	// })
	// .await
	// .unwrap();

	todo!();
}
