use chirp_worker::prelude::*;
use proto::backend::pkg::*;

#[worker_test]
async fn datacenter_update(ctx: TestCtx) {
	// msg!([ctx] cluster::msg::datacenter_update() {

	// })
	// .await
	// .unwrap();

	todo!();
}
