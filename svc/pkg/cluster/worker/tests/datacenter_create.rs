use chirp_worker::prelude::*;
use proto::backend::pkg::*;

#[worker_test]
async fn datacenter_create(ctx: TestCtx) {
	// msg!([ctx] cluster::msg::datacenter_create() {

	// })
	// .await
	// .unwrap();

	todo!();
}
