syntax = "proto3";

package rivet.backend.pkg.game.msg.update;

import "proto/common.proto";

/// name = "msg-game-update"
/// tail-ttl = 300
/// parameters = [
/// 	{ name = "game_id" },
/// ]
message Message {
	rivet.common.Uuid game_id = 1;
}

