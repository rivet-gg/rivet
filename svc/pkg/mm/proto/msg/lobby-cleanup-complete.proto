syntax = "proto3";

package rivet.backend.pkg.mm.msg.lobby_cleanup_complete;

import "proto/common.proto";

/// name = "msg-mm-lobby-cleanup-complete"
/// parameters = [
/// 	{ name = "lobby_id" },
/// ]
message Message {
	rivet.common.Uuid lobby_id = 1;
}

