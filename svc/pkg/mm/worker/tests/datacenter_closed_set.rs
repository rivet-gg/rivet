use chirp_worker::prelude::*;
use proto::backend::pkg::*;

#[worker_test]
async fn datacenter_closed_set(ctx: TestCtx) {
	// msg!([ctx] mm::msg::datacenter_closed_set() {

	// })
	// .await
	// .unwrap();

	todo!();
}
