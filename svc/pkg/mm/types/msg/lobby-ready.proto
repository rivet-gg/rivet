syntax = "proto3";

package rivet.backend.pkg.mm.msg.lobby_ready;

import "proto/common.proto";

/// name = "msg-mm-lobby-ready"
/// deduplicate = true
/// parameters = [
/// 	{ name = "lobby_id" },
/// ]
message Message {
	rivet.common.Uuid lobby_id = 1;
}

