syntax = "proto3";

package rivet.backend.pkg.nomad.msg.monitor_node_drain_complete;

import "proto/common.proto";

/// name = "msg-nomad-monitor-node-drain-complete"
/// parameters = [
/// 	{ name = "server_id" },
/// ]
message Message {
	rivet.common.Uuid server_id = 1;
	string node_id = 2;
}
