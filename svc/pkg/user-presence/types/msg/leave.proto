syntax = "proto3";

package rivet.backend.pkg.user_presence.msg.leave;

import "proto/common.proto";

/// name = "msg-user-presence-leave"
/// parameters = [
/// 	{ name = "user_id" },
/// ]
message Message {
	rivet.common.Uuid user_id = 1;
}
