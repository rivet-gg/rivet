syntax = "proto3";

package rivet.backend.pkg.job_run.msg.started;

import "proto/common.proto";

/// name = "msg-job-run-started"
/// parameters = [
/// 	{ name = "run_id" },
/// ]
message Message {
	rivet.common.Uuid run_id = 1;
}

