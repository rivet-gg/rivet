syntax = "proto3";

package rivet.backend.pkg.job_run.msg.cleanup_complete;

import "proto/common.proto";

/// name = "msg-job-run-cleanup-complete"
/// parameters = [
/// 	{ name = "run_id" },
/// ]
message Message {
	rivet.common.Uuid run_id = 1;
}

