syntax = "proto3";

package rivet.backend.pkg.user.msg.mutual_follow_delete;

import "proto/common.proto";

/// name = "msg-user-mutual-follow-delete"
/// tail-ttl = 300
/// parameters = [
/// 	{ name = "user_id" },
/// ]
message Message {
	rivet.common.Uuid user_a_id = 1;
	rivet.common.Uuid user_b_id = 2;
}
