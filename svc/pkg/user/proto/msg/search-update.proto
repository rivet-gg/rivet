syntax = "proto3";

package rivet.backend.pkg.user.msg.search_update;

import "proto/common.proto";

/// name = "msg-user-search-update"
/// parameters = [
/// 	{ name = "user_id" },
/// ]
message Message {
	rivet.common.Uuid user_id = 1;
}
