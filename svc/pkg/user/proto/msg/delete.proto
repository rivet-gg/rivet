syntax = "proto3";

package rivet.backend.pkg.user.msg.delete;

import "proto/common.proto";

/// name = "msg-user-delete"
/// parameters = [
/// 	{ name = "user_id" },
/// ]
message Message {
	rivet.common.Uuid user_id = 1;
}
