syntax = "proto3";

package rivet.backend.pkg.party.msg.update;

import "proto/common.proto";

/// name = "msg-party-update"
/// tail-ttl = 75
/// parameters = [
/// 	{ name = "party_id" },
/// ]
message Message {
	rivet.common.Uuid party_id = 1;
}

