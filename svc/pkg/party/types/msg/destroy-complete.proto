syntax = "proto3";

package rivet.backend.pkg.party.msg.destroy_complete;

import "proto/common.proto";

/// name = "msg-party-destroy-complete"
/// parameters = [
/// 	{ name = "party_id" },
/// ]
message Message {
	 rivet.common.Uuid party_id = 1;
}

