syntax = "proto3";

package rivet.backend.pkg.party.msg.invite_consume;

import "proto/common.proto";

/// name = "msg-party-invite-consume"
/// parameters = [
/// 	{ name = "query_id" },
/// ]
message Message {
	// Unique identifier for this consumption. Used to track the response
	// message.
	rivet.common.Uuid query_id = 1;
	rivet.common.Uuid invite_id = 2;
}

