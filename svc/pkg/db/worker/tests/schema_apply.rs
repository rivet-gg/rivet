use chirp_worker::prelude::*;
use proto::backend::pkg::*;

#[worker_test]
async fn schema_apply(ctx: TestCtx) {
	// msg!([ctx] db::msg::schema_apply() {

	// })
	// .await
	// .unwrap();

	todo!();
}
