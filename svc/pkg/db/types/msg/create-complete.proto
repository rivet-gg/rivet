syntax = "proto3";

package rivet.backend.pkg.db.msg.create_complete;

import "proto/common.proto";

/// name = "msg-db-create-complete"
/// parameters = [
/// 	{ name = "database_id" },
/// ]
message Message {
	rivet.common.Uuid database_id = 1;
}
