{
	"$schema": "https://turbo.build/schema.json",
	"extends": ["//"],
	"tasks": {
		"build": {
			"env": ["VITE_APP_*"],
			"outputs": ["dist/**"]
		},
		"build:embedded": {
			"env": ["VITE_APP_*"],
			"outputs": ["dist/**"]
		},
		"dev": {
			"env": ["VITE_APP_*"],
			"persistent": true
		}
	}
}
