{
	"$schema": "https://turbo.build/schema.json",
	"extends": ["//"],
	"tasks": {
		"build": {
			"env": ["VITE_APP_*"],
			"outputs": ["dist/**"],
			"dependsOn": ["@rivet-gg/actor-client#build"]
		},
		"dev": {
			"env": ["VITE_APP_*"],
			"persistent": true,
			"dependsOn": ["@rivet-gg/actor-client#build"]
		}
	}
}
