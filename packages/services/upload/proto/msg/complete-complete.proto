syntax = "proto3";

package rivet.backend.pkg.upload.msg.complete_complete;

import "resources/legacy/proto/common.proto";

/// name = "msg-upload-complete-complete"
/// parameters = [
/// 	{ name = "upload_id" },
/// ]
message Message {
	rivet.common.Uuid upload_id = 1;
}

