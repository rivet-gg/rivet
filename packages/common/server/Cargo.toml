[package]
name = "rivet-server"
version = "0.1.0"
edition = "2021"

[dependencies]
anyhow = "1.0.89"
global-error = { path = "../global-error" }
tokio = { version = "1.40", features = ["full", "tracing"] }
rivet-health-checks = { version = "0.1.0", path = "../health-checks" }
rivet-metrics = { version = "0.1.0", path = "../metrics" }
rivet-pools = { version = "0.1.0", path = "../pools" }
tracing = "0.1.40"
include_dir = "0.7.4"
rivet-config = { version = "0.1.0", path = "../config" }
tokio-cron-scheduler = "0.13.0"
chrono = "0.4.38"

