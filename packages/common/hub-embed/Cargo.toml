[package]
name = "rivet-hub-embed"
version = "0.1.0"
edition = "2021"

[build-dependencies]
reqwest = { version = "0.12.8", features = ["blocking"] }
zip = "2.2.0"

[dependencies]
include_dir = "0.7.4"

