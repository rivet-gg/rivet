[package]
name = "core-guard"
version = "0.1.0"
edition = "2021"

[features]
default = ["ops"]
ops = []

[dependencies]
chirp-workflow = { path = "../../../common/chirp-workflow/core" }
clickhouse-user-query = { path = "../../../common/clickhouse-user-query" }
once_cell = "1.19"
indoc = "2.0"
rivet-operation = { path = "../../../common/operation/core" }
serde = { version = "1.0", features = ["derive"] }